<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    
    <title>Docent</title>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post" >
                <input class="block w-full rounded-md px-5 py-2 bg-zinc-800 outline-none" name="title" type="text"  placeholder="Title">
                <textarea class="mt-2 w-full rounded-md block px-5 py-2 resize-none bg-zinc-800 outline-none" name="details" id="" cols="30" rows="10" placeholder="Write your task details...">

                </textarea>
                <input class="mt-2 bg-blue-500 px-5 py-2 rounded-md" type="submit" value="Create Task">
            </form> 
        </div>
        <div class="tasks flex gap-3 flax-wrap p-10">
            <%if(files.length > 0){%>
                <% files.forEach(file => { %>
                    <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-3xl tracking-tighter"><%=file%></h1>
                        <a class="text-blue-500 inner-block text-sm" href="/task">Read more.</a>
                    </div>
                <%})%>
            <%}else{%>                
                <h3 class="text-zinc-300">No tasks</h3>
                <%}%>
        </div>

    </div>
</body>
</html>